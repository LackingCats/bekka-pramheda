package net.nospacehuman.mbc.item.custom.tools.pickaxes;

import net.minecraft.item.PickaxeItem;
import net.minecraft.item.ToolMaterial;

public class TanzanitePickaxeItem extends PickaxeItem {
    public TanzanitePickaxeItem(ToolMaterial material, int attackDamage, float attackSpeed, Settings settings) {
        super(material, attackDamage, attackSpeed, settings);
    }
}
